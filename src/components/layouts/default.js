import React from "react"
import WideStretchedColumn from "../styled/WideStretchedColumn"

const Layout = ({ children }) => {
  return <WideStretchedColumn>{children}</WideStretchedColumn>
}

export default Layout
